version: 2


sources:
    - name: online_retail
      database: gcp-dbt-online-retail
      schema: raw_online_retail
      tables:
        - name: raw_online_retail
          identifier: raw_data
          config:
            loaded_at_field: updated_at
            freshness:
                warn_after: {count:12, period: month}
